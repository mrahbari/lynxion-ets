"""Data-integrity validation (E3.T10, gap-closure F5).

Canonical home for data-integrity validation, migrated from ``utils/`` so it is
a first-class adapter behind :class:`domain.ports.data_ports.DataIntegrityPort`.
"""
