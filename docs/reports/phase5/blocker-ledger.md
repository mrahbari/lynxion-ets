# Profitability Blocker Ledger (Phase 5)

Every discovered profitability blocker: impact, status, fix. Newest first.

| # | Blocker | Area | Impact | Status |
|---|---|---|---|---|
| B1 | **Market-impact unit bug** in `calculate_order_execution_price` (quote/base mismatch) inflated every entry ~5% → SL/TP wrong side → ~0% win across ALL strategies | E (execution) | CATASTROPHIC — destroyed ~all edge; aggregate BTC loss −189k | **FIXED** `f000ba8` |
| B2 | **Adapted-intent SL/TP inverted for longs** (domain vs infra `OrderSide` enum mismatch) → short geometry on longs → same-bar collisions → SL-priority losses | B/E | CATASTROPHIC — ~0% win on adapter strategies; collisions 472/472 | **FIXED** `584efd1` |
| B3 | Combined B1+B2 recovery, FULL 108-cell matrix: aggregate P&L −441,594 → −105,164 (~76% loss reduction); win rate 7.0%→11.9%. NOTE: loss-reduction (artifact recovery), NOT profit — 0/108 cells positive, 0 GO | — | quantified | **MEASURED** (POST matrix complete 108/108; baseline FROZEN 20260611; verdict NOT READY) |
| B4 | **Short positions not SL/TP-tracked** — `_execute_from_intent` SELL-when-flat opens a short without `active_positions.append` → shorts never hit SL/TP, only force-close | B/E | strategies that short can't be evaluated on short trades; long-only effectively | **OPEN** (follow-up; longs now correct) |
| B5 | **Signal/trade correspondence guard** (>80% of signals must have trades) incompatible with disciplined strategies that filter most signals | evaluation | was fatal-abort (fixed to warn); now noisy | mitigated (non-fatal) |
| B6 | **Strategy signals were a shared generic RSI mock** (all adapters identical) — pre Option-A | evaluation | per-strategy triage not credible | **FIXED** `e3819f4` (Option A per-strategy signals) |
| B7 | **SL/TP placeholder, adverse R:R geometry** — CONFIRMED E-P5.3 (8355 trades): mean reward leg **0.51R vs 1R risk** (R:R<1), payoff 0.11–0.32, 62% of TP hits still net a loss; SL exits realise **−1.67R** not −1R; cost drag **~0.6R/trade** (stops too tight vs fees+slippage) | B (lifecycle) | structural: per-strategy net exp −0.84R to −1.40R; no strategy can break even at realised 12–28% win rates | **CONFIRMED/QUANTIFIED** (E-P5.3) — remediation deferred (closure mode) |
| B8 | **Live trade-lifecycle logic never called** (breakeven/trailing/partial/time-stop) — E-P5.3: real but **SECONDARY**; counterfactual upside is an inflated upper bound vs cost-laden realised R and **cannot rescue gross-negative strategies** | B (lifecycle) | cannot create edge absent gross-positive expectancy; lower priority than entry edge + geometry | **QUANTIFIED (secondary)** (E-P5.3) |
| B14 | **Negative GROSS expectancy** — CONFIRMED E-P5.3: all 11 evaluable strategies lose **before** fees+slippage (gross −0.25R to −0.72R) on the bug-free execution path → entry signals lack realised directional edge | A/B (signal) | PRIMARY economic blocker: perfect free execution still loses; trade-mgmt/cost fixes cannot create edge | **OPEN** (primary; entry-edge question) |
| B15 | **Effectively long-only** — E-P5.3 dump: 8380/8380 trades are longs (side=sell close); shorts never produce SL/TP lifecycle records (consistent with B4) | B/E | half the directional space unevaluated | **OPEN** (= B4 manifestation) |
| B9 | **MTF `compute_trend()` returns mock 0**; timeframe weights unused → counter-HTF entries not gated | C (MTF) | **IMPACT REFUTED (E-P5.4):** 30% of longs are counter-trend, but counter-trend longs are the *least* unprofitable bucket (−1.031R) vs trend-aligned the *worst* (−1.174R); a naive HTF gate WORSENS expectancy by 0.027R/trade. No evidence MTF conflict is a profitability leak (entry_regime proxy). De-prioritised | **OPEN but de-prioritised** (no measured benefit) |
| B10 | **Portfolio correlation/heat/concentration offline-only**; live sizing ignores other positions; concentration hardcoded False | D (portfolio) | **CONFIRMED/QUANTIFIED (E-P5.4):** BTC/ETH/SOL avg corr **0.83**, only **1.25 effective bets** of 3, long-all-three carries **1.60×** naive-diversification volatility → stacked correlated-long risk. RISK amplifier, NOT an expectancy leak (can't rescue B14) | **CONFIRMED** → remediation deferred (closure mode) |
| B11 | **SL/TP not structure-aware** (ATR-only) → stops in obvious liquidity pools (stop hunts) | E/B | stop-out-then-reverse losses | **OPEN** → E-P5.4 T3 |
| B12 | **OHLCV-only** — all microstructure features (sweep/absorption/imbalance/OI) are proxies/stubs (`detect_sweep`→0, OI `*1.5`) | E (microstructure) | E-P5.5: 4 micro-named strategies (sweep_scalper/oi_footprint/liquidity/vwap_reversal) run on degraded signals → INSUFFICIENT EVIDENCE for their hypotheses (not disproof); needs L2/trades/OI/funding to test | **CONFIRMED (insufficient-evidence)** (E-P5.5) |
| B13 | **Learning loop open** — `recalibrate_classifier()` is a print stub; adaptive weights not confirmed to persist | F (adaptation) | E-P5.5: no observable within-run regime adaptation (expectancy uniformly −1.0..−1.2R across regimes); moot while gross edge absent (B14) | **CONFIRMED (stub, moot)** (E-P5.5) |
| B16 | **Cost cliff from tight stops (microstructure)** — E-P5.5: total execution cost **0.677R/trade (~10 bps notional)** is a *normal* cost stack, catastrophic only because stops avg **17 bps** wide; bridges gross −0.42R→net −1.09R; sensitivity ranks by stop tightness (scalping 0.74R … mean_reversion 0.55R) | E/B (microstructure) | largest mechanical drag, but SYMPTOM of B7 tight stops, not mispriced spread/slippage | **CONFIRMED/QUANTIFIED** (E-P5.5; = B7 manifestation) |

Also tracked elsewhere: 2 pre-existing strategy bugs fixed (scalping int/dict,
volatility_breakout impossible breakout) — see `tasks/backtest-strategy-signal-fidelity/`;
DIRECTIONAL_NO_GO verdict added; discipline made backtest-time-aware + outcome feedback.
