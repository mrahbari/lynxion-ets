# E5.T4 — `auto_detection_orchestrator.py` Decomposition Discovery (read-only)

E5-A, step 6 · DISCOVERY ONLY. Target: `infrastructure/orchestrators/auto_detection_orchestrator.py`
(1289 LOC, single class `AutoDetectionOrchestrator`). **No code changed.**

## ★ Classification: APPROVAL REQUIRED (not low-risk autonomous work)
Unlike E5.T3 (a dead-duplicate deletion), this is a **live god module** with real coordination
**and trading-execution** logic. There is **no** pre-extracted-service duplication to delete
(the constructor wires concrete infra directly; `architecture_orchestrator` only has
start/stop/get_status and does not duplicate these methods). The E5.T4 goal — *move coordination
into a NEW `application/pipelines/detection_pipeline.py`, depend on ports not concrete infra* —
triggers these AUTONOMY-RULE stop conditions:
- **#6 Architectural scope** — creates `application/pipelines/` (does not exist) + inverts infra→port dependencies.
- **#4 Trading-execution behavior** — `_execute_trade_from_intent` (~290 LOC) builds and places orders (ccxt), dedup-checks broker active orders.
- **#5 Order/Money semantics** — that path constructs `Order`, `OrderSide`, `Money`, `Decimal`.
- **#7 MEDIUM/HIGH risk** — task file rates this HIGH.
- **#8 Multiple valid directions** — how to split a 1289-LOC orchestrator (what moves to application vs stays in infra) is a genuine design choice.

→ **Stopping for approval and a chosen direction before any extraction.**

## Liveness / importers
LIVE: container `auto_detection_orchestrator_factory` (`bootstrap/container.py:485`) → injected into
`application/use_cases/run_auto_detection.py:48` and `interface/cli/trading_system_production.py:73`
(+ an e2e characterization test). Calls `run_auto_detection()`.

## Public API (MUST preserve — condition #1)
- `__init__(market_data_repo: DataProviderPort, execution_service: ExecutionPort, portfolio_service: PortfolioManagementPort, optimization_service: IOptimizationService, symbols: List[str], risk_config=None, comprehensive_logging=True)`
- `initialize_system()`, `run_auto_detection()`, `stop_system()`
- Collaborators built in `__init__`: `MarketOpportunityWatcher` (E5.T3'd; uses `.start_monitoring`/`.stop_monitoring`/`.symbols`), engine/fusion/strategy services, `event_router`, `PendingOrdersTracker`, `RiskAlertService`.

## Responsibility map (18 methods)
| Cluster | Methods | Layer fit |
|---|---|---|
| Lifecycle / public | `__init__`, `initialize_system`, `run_auto_detection`, `stop_system` | orchestrator shell |
| Background loops | `_start_background_services`, `_opportunity_processing_loop` (146–292), `_risk_monitoring_loop` | **coordination → application/pipelines** (port-typed) |
| Opportunity handling | `_handle_opportunity`, `_calculate_opportunity_score`, `_execute_strategy_for_opportunity` (376–555) | coordination (mostly) |
| Execution-intent flow (**TRADING**) | `_handle_execution_intent_event`, `_check_duplicate_execution_intent`, `_check_broker_active_orders_for_duplicate`, `_remove_pending_execution_intent`, **`_execute_trade_from_intent` (879–1169, ~290 LOC: Order/Money/ccxt/place)**, `_check_risk_acceptance_from_intent` | **infrastructure** (broker/exchange) — must NOT move to application |
| Util | `_get_symbol_lock` | shell |

## Dependency / E4-frozen notes
- Imports `domain.ports.*` (DataProviderPort/ExecutionPort/PortfolioManagementPort/IOptimizationService), `domain.value_objects.Symbol`, and inline `domain.entities.Order/OrderSide` + `Money` in the execution path. **No `application.dto` import** — but the trading-execution path constructs domain `Order`/`Money` directly, so any change there is condition #4/#5 and must keep E4 frozen.
- Inline-imports concrete infra (engine_service, fusion_service, strategy_manager, architecture_orchestrator, PendingOrdersTracker) — the dependency-inversion-to-ports part of the goal is real architectural work.

## Candidate directions (for your decision — condition #8)
- **A. Coordination-extract (task-literal):** move the loops + opportunity/intent *coordination* into `application/pipelines/detection_pipeline.py` typed on ports; leave execution mechanics (`_execute_trade_from_intent`, broker dedup) in an infra execution component. Highest fidelity to E5.T4; largest blast radius; touches trading paths.
- **B. Infra-only structural split (no layer move):** split the 1289-LOC class into focused **infra** modules (loops / opportunity / execution-intent / trade-exec) without moving to application or inverting to ports — pure mechanical extraction preserving signatures. Lower risk; defers the architectural inversion.
- **C. Minimal:** extract only the self-contained, non-trading helpers (`_calculate_opportunity_score`, `_get_symbol_lock`, dedup checks) into a helper module; leave the trading/coordination core intact. Lowest risk; partial LOC reduction.
- **D. Defer E5.T4** behind a test-capable venv (behavior-parity for the live trading path is NOT runnable here — no numpy/pandas/pytest), pairing it with the deferred E4 broker VO-wiring.

## Risk
HIGH — live auto-detection→execution coordinator on the production trading path. Behavior-parity
cannot be verified in this environment (no numpy/pandas/pytest). No low-risk dead/delegated subset
exists to execute autonomously.

## Recommendation
Approve a **direction** first. I recommend **B then A in stages** (or **C** if minimizing risk now):
do a behavior-preserving infra-internal split (B/C, mechanical, signatures preserved) — which is
closer to autonomous-eligible — and treat the application/pipelines move + port inversion (A) as a
separately-approved architectural step with a venv parity check. **Awaiting your choice; no code changed.**
