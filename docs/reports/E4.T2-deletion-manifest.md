# E4.T2 — Deletion Manifest (STAGED — awaiting approval)

**Purpose:** resolve E4.T2 by removing the `shared.types` shadow model + its dead consumer
cluster, achieving `grep -rn "shared.types" == 0` with a green suite. **Nothing has been
deleted yet** — this manifest is for review/approval. Chosen path: *stage deletion manifest
first* (given the prior verified-dead engine deletion was reverted).

## A. Files to `git rm` (20)

The shadow model + every **dead** `shared.types` consumer. Closure verified for **both**
dotted-path and **relative** imports — each has *no external live/test importer* (intra-set
only).

```
shared/types.py
application/backtesting/backtest_engine.py
infrastructure/backtest/adapters/simulator.py
infrastructure/brokers/adapters/base.py
infrastructure/execution/adapters/executor.py
infrastructure/execution/adapters/vwap.py
infrastructure/execution/adapters/twap.py
infrastructure/execution/adapters/smart_router.py
infrastructure/fusion/adapters/fusion_v3.py
infrastructure/portfolio/adapters/allocator.py
infrastructure/portfolio/adapters/risk_parity.py
infrastructure/portfolio/adapters/volatility_target.py
infrastructure/risk/adapters/drawdown.py
infrastructure/risk/adapters/exposure.py
infrastructure/risk/adapters/governor.py
infrastructure/risk/adapters/kelly.py
infrastructure/strategies/adapters/adaptive_selector.py
infrastructure/strategies/adapters/base_strategy.py
infrastructure/strategies/adapters/health_system.py
infrastructure/strategies/adapters/router.py
```

## B. File to EDIT (1, kept — live via WFO path)

`infrastructure/backtest/adapters/walk_forward.py` — currently the only live-reachable
`shared.types` referrer and the only live relative-importer of `simulator.py`. Both are
**dead usages**: `Signal`/`Order` are imported but never used; `BacktestSimulator` is only a
default-arg value that the method ignores (it hardcodes `RealisticBacktester` at runtime).

Proposed minimal edits (no runtime behavior change):
```diff
-from shared.types import Signal, Order            # line 2  (unused names)
 ...
-from .simulator import BacktestSimulator          # line 7  (deleted module)
 ...
-                                  backtester_class: type = BacktestSimulator) -> Dict:
+                                  backtester_class: Optional[type] = None) -> Dict:
```
`Optional` is already imported in that file. The sole caller
(`application/walk_forward/wfo_orchestrator.py:114`) does **not** pass `backtester_class`, and
the method never uses it internally → safe.

## C. Dry-run impact / safety proof

- **Closure (dotted-path):** 19/19 dead consumers → only intra-set/self importers; `shared/types.py` → only `walk_forward.py`.
- **Closure (relative-import) — extra pass that caught two cases:**
  - `simulator.py` ← `walk_forward.py` (`from .simulator import BacktestSimulator`) → handled by edit (B).
  - `base.py` ← `advanced_optimization_service.py:351` = **FALSE POSITIVE** (`from hyperopt import base`, the third-party lib — not our `base.py`).
- **Live broker adapters** (`bingx/binance/mexc/phemex_adapter.py`) and the flat live
  `broker_adapters.py` do **not** import `brokers/adapters/base.py`.
- **Live strategy adapters** (`*_strategy_adapter.py`) extend the flat
  `strategies/strategy_adapters.py:BaseStrategyAdapter`, **not** the dead `adapters/base_strategy.py`.
- **Package `__init__.py`** in every affected `/adapters/` dir does not re-export any deleted
  module (`brokers/adapters/__init__.py` and `strategies/adapters/__init__.py` reference only
  LIVE adapters; the rest are empty).
- **Tests:** 0 imports of `shared.types` or any deleted module (`tests/`).
- **Non-`.py` references** to `shared.types`: none.
- `application/backtesting/backtest_engine.py` (dead, 0 importers) imports the deleted
  `router` + `shared.types` → consistently removed in the same set.

**Post-deletion expectation:** `grep -rn "shared.types\|from shared import types"` → 0;
imports resolve; suite green (validated via `py_compile` + container `resolve_all()` + domain
import, given the env's pandas/pytest limits).

## D. Out of scope (explicitly NOT touched)

- `infrastructure/backtest/adapters/walk_forward.py` — **kept** (live WFO), edited only as in (B).
- `infrastructure/backtest/adapters/main.py` — kept (not a `shared.types` consumer; separate concern).
- `OrderType` — no live consumer; **not** added to `domain` (per prior decision). It vanishes with `shared/types.py`.
- `main_hexagonal_container.py` — dead, but not a `shared.types` consumer; leave for E8.
- All LIVE `*_strategy_adapter.py`, broker adapters, flat `*_adapters.py` files — untouched.

## E. Execution plan once approved (single reviewable commit)

1. `git rm` the 20 files in (A).
2. Apply the 3 edits to `walk_forward.py` (B).
3. Verify: `grep -rn "shared.types" .` → 0; `py_compile`; container `resolve_all()`; domain import.
4. Commit on `e3-consolidation-behind-ports`:
   `E4.T2: delete shared.types shadow model + dead adapter cluster (P6); neutralize walk_forward dead deps`.

**STOP — awaiting your approval of this manifest before any `git rm`.**
