# E5.T3 — `market_opportunity_watcher.py` Decomposition Discovery (read-only)

E5-A, step 5 · Phase 1 DISCOVERY ONLY (no code changes, no moves, no commit).
Target: `infrastructure/watchers/market_opportunity_watcher.py` (1778 LOC, single class
`MarketOpportunityWatcher(BaseWatcher)`).

## ★ Decisive finding — the split ALREADY happened; this is a DEAD-DUPLICATE problem, not a split
The decomposition into focused services has **already been done**:
`SymbolDiscoveryService` (643), `SymbolValidationService` (166),
`WatcherInitializationService` (408), `MonitoringAnalysisService` (528) — and these services
hold **their own live copies** of the logic. The 1778-LOC class still contains the **original,
now-superseded duplicates**. The constructor and `start/stop_monitoring` **delegate to the
services**; the in-class duplicate clusters have **no live caller**.

→ **E5.T3 is best executed as dead-code reconciliation (delete the superseded in-class
duplicates), NOT as a new extraction.** This collapses the file to a ~130-LOC thin
orchestrator and is far lower-risk than splitting.

## 1. Importer graph
- **`market_opportunity_watcher` ← `infrastructure/orchestrators/auto_detection_orchestrator.py`** (sole importer/instantiator, line 66–67).
- Orchestrator uses only: `self.opportunity_watcher.symbols` (attr, :77), `.start_monitoring()` (:132), `.stop_monitoring()` (:1245). **No other public method is used externally.** (`get_status` not called by the orchestrator/use-case path.)
- Watcher → imports: domain entities/VOs, `BaseWatcher`, ports (`SignalPort`,`FusionPort`), `event_router`, the **4 services**, utils, settings; heavy inline `ccxt`/`requests` (in the dead clusters); dynamic sub-watcher adapter imports (in the dead init path / now in `WatcherInitializationService`).

## 2. Responsibility map (current methods → owner)
| Cluster | Watcher methods (lines) | Live owner today | Status in watcher |
|---|---|---|---|
| Symbol validation | `_filter_stablecoin_pairs` (92), `_validate_symbol_data_availability` (171) | `SymbolValidationService` | **dead dup** |
| Symbol discovery | `_discover_symbols_automatically` (235) + ~18 `_discover_*` / `_get_active_symbols_from_exchange` / `_update_symbol_list` / `start_periodic_symbol_updates` (235–1274) | `SymbolDiscoveryService` (+ periodic in `MonitoringAnalysisService`) | **dead dup (~1180 LOC)** |
| Watcher init | `_initialize_watchers` (1275) | delegates → `WatcherInitializationService` + `monitoring_service.set_watchers_and_symbols` | **LIVE (thin)** |
| Monitoring/analysis | `_monitoring_loop` (1296) + `_check_market_opportunities`, `_update_watchers_with_market_data`, `_format_market_data_for_watcher`, `_initialize_watcher_histories_with_historical_data`, `_analyze_symbol`, `_get_watcher_priority_order`, `_should_skip_remaining_watchers`, `_process_opportunities`, `_execute_intent_trade` (1296–1769) | `MonitoringAnalysisService` (has its own copies) | **dead dup (~470 LOC)** |
| Lifecycle / status | `__init__` (34), `start_monitoring` (1286), `stop_monitoring` (1291), `get_status` (1770) | the watcher (delegating) | **LIVE** |

## 3. Public API inventory
**Externally used (must preserve byte-for-byte):**
- `MarketOpportunityWatcher(symbols=…, opportunity_callback=…, auto_discover_symbols=…, comprehensive_logging=…, market_data_repo=…, event_router=…)` — constructor.
- attribute `.symbols`
- `.start_monitoring()`, `.stop_monitoring()`
**Defined but not externally called:** `.get_status()` (keep — cheap, public), `.start_periodic_symbol_updates()` (dead), all `_*` methods (private; dead except `_initialize_watchers`).

## 4. Stateful / shared-data analysis
- Instance state set in `__init__` and used on the LIVE path: `self.symbols`, `self.watchers`, `self.broker_service`, `self.last_observations`, `self.event_router`, `self.market_data_repo`, the 4 service handles, `self.logger`, `self.is_running` (init `False`).
- **`self.is_running`** is only ever flipped to `True` inside the **dead** `_monitoring_loop`; the live `start_monitoring` delegates to the service and does **not** set it → `get_status()['is_running']` already reports `False` on the live path today (pre-existing; deletion does not change this).
- `self.last_observations` / `self.watchers` are read by `get_status`; populated via `_initialize_watchers` (watchers) — `last_observations` is written only in dead monitoring code (so it stays `{}` on the live path today — pre-existing).
- No module-level mutable shared state beyond the imported `event_router` global.

## 5. Dependency analysis
- **No external code calls any in-class duplicate method** (repo-wide sweep): the matches for `_monitoring_loop`/`_check_market_opportunities`/`_analyze_symbol`/`_update_symbol_list`/… resolve to the **services' own definitions** (`monitoring_analysis_service.py`) or **unrelated classes** (`strategy_manager.py`, `watcher_manager.py` each have their own `_monitoring_loop`). The services do **not** import the watcher.
- `start_periodic_symbol_updates` (watcher:1004) — only callers are `MonitoringAnalysisService`'s own copy (:46→:502). Watcher copy unreachable.
- **No dynamic dispatch on method names** in the watcher (only `getattr(self,'_watcher_specific_symbols',None)` — an attribute). So static deadness is reliable.
- Frozen-layer check: the watcher imports `domain.entities`/`domain.value_objects`/`domain.ports` (read-only use) — **deletion touches none of `domain/`, `application/dto/`, ingestion, or broker**.

## 6. Candidate boundaries (reframed: deletion units, not new modules)
The decomposition target already exists (the 4 services). The boundaries are the **dead
clusters to remove** from the watcher:
- **B1:** validation dup — `_filter_stablecoin_pairs`, `_validate_symbol_data_availability`.
- **B2:** discovery dup — `_discover_symbols_automatically` … `_discover_by_time_based_rotation`, `_get_active_symbols_from_exchange`, `_update_symbol_list`, `start_periodic_symbol_updates` (+ nested `update_loop`).
- **B3:** monitoring dup — `_monitoring_loop` + its 9 helpers.
Result: watcher = `__init__` + `_initialize_watchers` + `start_monitoring` + `stop_monitoring` + `get_status` (~130 LOC).
*(If a true “split into modules” is required instead, the services ARE those modules — no new files needed; E5.T3 would just be the deletion + confirming the services are the canonical homes.)*

## 7. Risk classification
- **Overall: LOW–MEDIUM** (deleting verified-unreachable duplicates) — *not* the HIGH of a live 1778-LOC split.
- **B1 / B2 (validation + discovery dup): LOW** — no live caller; logic lives in the services.
- **B3 (monitoring dup): LOW–MEDIUM** — large, and conceptually central; must re-confirm `_monitoring_loop` stays unreferenced and that `get_status` doesn’t read state only the dead code wrote (it reads `is_running`/`last_observations`, already inert on the live path — preserve current values, see §4).
- **Latent risk flagged:** the watcher and `MonitoringAnalysisService`/`SymbolDiscoveryService` are **divergent duplicates** — a correctness hazard if anyone ever wired the watcher copies. Deletion removes the hazard.

## 8. Safe sequence (deletion, behavior-preserving)
1. **Re-verify deadness** (grep `self.<method>` within the file + repo-wide external callers + no getattr/string dispatch) — already done here; re-run immediately before editing.
2. **B1** delete validation dup → compile + import-graph.
3. **B2** delete discovery dup (incl. `start_periodic_symbol_updates`) → compile + import-graph.
4. **B3** delete monitoring dup → compile + import-graph.
5. Confirm remaining watcher = thin orchestrator; `__init__`, `_initialize_watchers`, `start/stop_monitoring`, `get_status` unchanged in signature/behavior.
6. Trim now-unused imports (ccxt/requests/threading/time were used only by deleted clusters) — import cleanup only.

## 9. Validation strategy
- Per step: `py_compile` of the watcher + `auto_detection_orchestrator`; import-graph (no dangling); repo grep that deleted method names have no external caller.
- **Behavior parity (REQUIRED, not runnable here):** in a test-capable venv — construct `MarketOpportunityWatcher` with a fixture symbol set; assert `.symbols`, `.get_status()` shape, and `start/stop_monitoring()` delegate to `MonitoringAnalysisService` identically (mock the service, assert call-through). The watcher-fixture replay named in the task file (“identical opportunity set”) is satisfied because opportunity generation already runs entirely in `MonitoringAnalysisService` (unchanged).
- Invariants: `domain/` pandas-free, `shared.types`==0 (unaffected).

## 10. Rollback strategy
- Each boundary (B1/B2/B3) deleted in its own commit → `git revert <sha>` restores that cluster independently.
- The watcher is behind one importer (`auto_detection_orchestrator`) and the public surface is unchanged, so a full rollback is a single-file restore (`git checkout <baseline> -- market_opportunity_watcher.py`).
- Pre-step tag/baseline = current `fb6dafa`.

## Recommendation
Execute E5.T3 as a **staged dead-duplicate deletion** (B1→B2→B3), each its own commit with the
parity check above, preserving the 5-member public surface exactly. This delivers the LOC
reduction E5.T3 targets with minimal blast radius and removes a real divergent-duplicate hazard.
**Do NOT implement yet — awaiting approval.**
